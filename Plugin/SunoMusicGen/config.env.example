# Suno API配置
# 从 https://sunoapi.org/api-key 获取您的API密钥
# 🔑 多API Key支持
# 可以配置多个API Key，用英文逗号分隔
# 插件会自动轮换使用，避免单个Key的限制
# 遇到429（积分不足）或430（频率过高）错误时会自动切换到下一个Key
# 示例：SUNO_API_KEY=key1,key2,key3
SUNO_API_KEY=your_api_key_here

# API基础URL（通常不需要修改）
SUNO_API_BASE_URL=https://api.sunoapi.org

# 轮询配置
POLLING_INTERVAL_MS=10000  # 轮询间隔（毫秒），默认10秒
MAX_POLLING_ATTEMPTS=60     # 最大轮询次数，默认60次（10分钟）

# 可选：回调URL（需要公网可访问的webhook服务器）
# 注意：这是给Suno API服务器回调用的，需要公网地址
# 当前插件使用轮询方式，不依赖回调，可以不配置
# CALLBACK_URL=https://your-public-server.com/suno-webhook